# GitHub 新闻同步配置

## 仓库信息
- 仓库地址: 
- 分支: main (或 main)
- 文件存放路径: / (根目录)

## 认证方式（选择一种）

### 方式1: Personal Access Token (推荐)
1. 访问 https://github.com/settings/tokens
2. 生成新的 token (classic)，勾选 `repo` 权限
3. 将 token 提供给我

### 方式2: SSH Key (如果已有配置)
- 检查是否已配置 SSH: `ls ~/.ssh/id_rsa.pub`

## 当前状态
- 本地 Git 已初始化
- 需要配置 remote origin
- 需要设置用户认证

---
请在下方填写仓库地址和 token，或选择其他方式
